<!DOCTYPE html>
<html lang="en">
{% include "head_htmx.html.j2" %}
<body style="overflow: auto; display: block; background-image: url({{ url_for('static', path='background_clown_call.png') }}); background-size: cover;">
    {% include "responses/audio_alert.html.j2" %}
    <div id="alerts" class="fixed top-0 right-0 m-8 space-y-4"></div>

    <div class="px-8" hx-ext="ws" ws-connect="/ws/">
        <h1 class="text-4xl font-bold">{{ department_name }}</h1>
        <h2 class="text-2xl font-bold">{{ department_descriptive_name }}</h2>
        <table class="min-w-[50%] mb-4 bg-gradient-to-r from-indigo-500 to-pink-500 w-9/10 mx-auto">
            <thead class="border-b text-lg">
                <tr>
                    <th class="border" p-1>Clowns-Team</th>
                    <th class="border" p-1>Zeit</th>
                    <th class="border" p-1>gesendet</th>  
                    <th class="border" p-1>empfangen</th>
                </tr>
            </thead>

                <tbody class="text-center text-sm" id="messages-responses">
                </tbody>
        </table>

        <form class="mt-4 w-9/10 mx-auto" id="form" ws-send>
            <div class="flex">
                {% include "responses/empty_message_input.html" %}
                <button class="bg-blue-300 py-2 px-4 rounded-r-lg ml-2" ws-send>send</button>
            </div>
        </form>
    </div>

</body>
</html>